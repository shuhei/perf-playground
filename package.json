{
  "name": "perf-test",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "sudo docker build . -t perf-test",
    "build-alpine": "sudo docker build . -f Dockerfile.alpine -t perf-test-alpine",
    "start": "sudo docker run --rm -itd -p 8080:8080 perf-test",
    "start-alpine": "sudo docker run --rm -itd -p 8080:8080 perf-test-alpine"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "lru-cache": "^4.1.3",
    "react": "^16.4.1",
    "react-dom": "^16.4.1"
  },
  "devDependencies": {
    "0x": "^4.1.3",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1"
  }
}
